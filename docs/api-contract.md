# API contract

No HTTP endpoint, route handler, Server Action, event, or public integration is defined in the foundation phase.

Future contracts must state authentication and authorization, ownership, input and output schemas, status and safe-error behavior, idempotency, rate limits, side effects, atomicity, and tests. The server remains authoritative; client types alone are not validation.
