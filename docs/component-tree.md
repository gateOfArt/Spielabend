# Component tree

Current foundation rendering tree:

```text
RootLayout (Server Component, src/app/layout.tsx)
└── Home (Server Component, src/app/page.tsx)
    └── main
        └── section
```

There are no Client Components or reusable product components yet. Update this tree when an approved PRD introduces them.
