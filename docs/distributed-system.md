# Distributed-system boundary

The foundation is one Next.js deployable with no selected database, queue, worker, third-party integration, or cross-service protocol. It is not currently a distributed system.

If a future PRD introduces a remote boundary, document ownership of truth, consistency, timeouts, retries, idempotency, ordering, duplicate delivery, atomicity, observability, privacy, failure recovery, and test strategy before implementation.
